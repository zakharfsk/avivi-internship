# in ngrok.yml
version: 2
tunnels:
  nginx:
    addr: nginx:80
    schemes:
      - https
    host_header: "avivi-internship.ngrok.dev"
    proto: http
    inspect: true
    domain: avivi-internship.ngrok.dev